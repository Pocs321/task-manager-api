---
description: Run linter and fix issues
---

Run the linter and fix any issues found.

## Lint Output

```
$(`npm run lint 2>&1 || true`)
```

Fix all auto-fixable issues. For issues that require manual intervention, list them with file paths and line numbers.
